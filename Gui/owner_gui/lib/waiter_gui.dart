import 'package:flutter/material.dart';

class WaiterPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Waiter Page'),
      ),
      body: Center(
        child: Text('Waiter Gui'),
      ),
    );
  }
}