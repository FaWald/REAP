// Import statement for Flutter's Material package.
import 'package:flutter/material.dart';

// AppColors class definition.
// This class is used to define and centralize the color scheme of the application.
class AppColors {
  // Defining a static constant for background color.
  // This color is primarily used for the background of scaffolds (screens) in the app.
  static const Color backgroundColor = Color(0xFFFFFFFF); // White color for background.

  // Defining a static constant for button color.
  // This color is used for the various buttons in the app to maintain a consistent look.
  static const Color buttonColor = Color(0xFF8246C5); // Purple color for buttons.

  // Defining a static constant for text color.
  // This color is used for the text displayed across the app to ensure readability.
  static const Color textColor = Color(0xFF000000); // Black color for text.
}
